# Fill and rename/copy to .env

# From Datasphere Space → Settings → Database Access
DSP_HOST=your-hostname
DSP_PORT=your-port

# DB credentials (use least-privilege, read-only)
DSP_UID=GE223911
DSP_PWD=your-password

# Optional default schema (Open SQL schema)
DSP_SCHEMA=YOUR_SCHEMA

